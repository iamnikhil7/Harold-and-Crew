# Crew Artwork

Drop mascot group compositions here — e.g. pre-arranged "crew lockups"
you want to reuse without composing them in CSS.

Recommended filenames:

- `landing-trio.png` — the three mascots shown in the landing top-left
- `archetype-trio.png` — the three mascots shown around the archetype reveal
- `peek-left.png`, `peek-right.png` — Harold peeking from a corner
